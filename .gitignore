# Compiled and build artifacts
__pycache__/
*.pyc
*.py[cod]
*.egg-info/
*.egg
*.o
*.obj
dist/
build/
rust/target/

# Dependencies
.venv/
venv/
node_modules/
npm/.bin/

# Logs and temp files
*.log
*.tmp
*.swp
*.swo
*~

# Environment
.env
.env.local
.env.*
!.env.example

# Editors
.vscode/
.idea/

# Regenerated by scripts/record-demo-gif.sh (GIF is committed)
assets/demo.cast

# Project-specific (cuba-memorys)
.mypy_cache/
.pytest_cache/
.hypothesis/
.ruff_cache/
uv.lock
.fastembed_cache/
.claude/
.grok/
.mcp.json
.mcpregistry_*
.DS_Store
Thumbs.db

# Database backups (local only — never commit dumps)
backups/
!backups/.gitkeep

# Internal working files — kept local, never published.
# docs/ are work plans (not install docs); sandbox/ has local paths + brain refs.
docs/
sandbox/
