COMMENT ON COLUMN brain_relations.provenance IS
    'extracted = asserted via cuba_puente create, or read from source by codegraph; inferred = read out of free text by an LLM (cuba_ingesta auto_extract); predicted = persisted Adamic-Adar suggestion.';
